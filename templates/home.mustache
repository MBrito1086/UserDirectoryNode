<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>User Directory</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <h1>User Directory</h1>
    <ul>
      {{#users}}
      <li>
        <img src="{{avatar}}">
        <p><a href="/users/{{id}}">{{name}}</a></p>
        {{#job}}<span>{{job}}, {{company}}</span>{{/job}}
        {{^job}}<span class="forhire">Available for hire</span>{{/job}}
      </li>
      {{/users}}
    </ul>
    </script>
  </body>
</html>
